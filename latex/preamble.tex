
% --- Page layout & typography ---
\usepackage[a4paper,margin=2.5cm]{geometry}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}

% --- Math ---
\usepackage{amsmath, amssymb, amsthm}
\usepackage{mathtools}

% --- Lists / tables / graphics ---
\usepackage{enumitem}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{caption}

% --- Code listings ---
\usepackage{xcolor}
\usepackage{listings}
\usepackage{needspace}

\definecolor{codebg}{RGB}{245,245,245}
\definecolor{codekeyword}{RGB}{0,0,150}
\definecolor{codecomment}{RGB}{0,128,0}
\definecolor{codestring}{RGB}{163,21,21}

\lstdefinestyle{codebase}{
  backgroundcolor=\color{codebg},
  basicstyle=\ttfamily\small,
  keywordstyle=\color{codekeyword}\bfseries,
  commentstyle=\color{codecomment}\itshape,
  stringstyle=\color{codestring},
  numbers=left,
  numberstyle=\tiny\color{gray},
  stepnumber=1,
  numbersep=8pt,
  frame=single,
  framerule=0.5pt,
  breaklines=true,
  breakatwhitespace=true,
  columns=fullflexible,
  showstringspaces=false,
  tabsize=2,
  captionpos=b
}

\lstdefinestyle{cpp}{
  style=codebase,
  language=C++
}

\lstdefinestyle{python}{
  style=codebase,
  language=Python
}

\lstdefinestyle{pseudo}{
  style=codebase,
  language={},
  morekeywords={function,return,for,while,if,else,elif,in,from,to,downto,break,continue},
}

\lstset{style=codebase}
\captionsetup[lstlisting]{font=small}

% --- Prevent code blocks splitting across pages ---
% Use CodeBlock to guarantee "start on new page if it won't fit".
% #2 is an estimated line count (baselines).
\newenvironment{CodeBlock}[2][]{
  \Needspace{#2\baselineskip}
  \begin{lstlisting}[#1]
}{
  \end{lstlisting}
}

\newcommand{\InputCodeBlock}[3][]{%
  \Needspace{#2\baselineskip}%
  \lstinputlisting[#1]{#3}%
}

% --- Hyperlinks (load late-ish) ---
\usepackage{hyperref}
\hypersetup{
  colorlinks=true,
  linkcolor=blue,
  urlcolor=blue,
  pdftitle={Algorithms Portfolio},
  pdfauthor={Ryan McMillan}
}

% --- Header / footer ---
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[C]{Algorithms Portfolio}
\fancyfoot[L]{\small Ryan McMillan}
\fancyfoot[C]{\small \thepage}
\fancyfoot[R]{\small v0.1}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0.3pt}
\setlength{\headheight}{14pt}

% Ensure TOC and section-opening pages use the same style
\fancypagestyle{plain}{%
  \fancyhf{}%
  \fancyhead[C]{Algorithms Portfolio}%
  \fancyfoot[L]{\small Ryan McMillan}%
  \fancyfoot[C]{\small \thepage}%
  \fancyfoot[R]{\small v0.1}%
  \renewcommand{\headrulewidth}{0.4pt}%
  \renewcommand{\footrulewidth}{0.3pt}%
}

% --- Build toggles: personal notes ---
\newif\ifpersonalnotes
\personalnotestrue
% \personalnotesfalse  % Exemplars / Quant Prep builds

\ifpersonalnotes
  \newenvironment{Thinking}{
    \par\vspace{0.6em}
    \noindent\textbf{Author's Thinking.}
    \begin{quote}\small
  }{
    \end{quote}\vspace{0.6em}
  }
\else
  \newenvironment{Thinking}{}{}
\fi

% --- Problem entry macros ---
\newcommand{\ProblemTitle}[1]{\subsection{#1}}

\newcommand{\ProblemMeta}[3]{%
  \noindent\textbf{Difficulty:} #1 \hfill \textbf{Tags:} #2 \hfill \textbf{ID:} #3\par\vspace{0.4em}%
}

\newcommand{\Block}[1]{\par\noindent\textbf{#1}\quad}

\newcommand{\ProblemSummary}{\Block{Problem.}}
\newcommand{\KeyIdea}{\Block{Key idea.}}
\newcommand{\Algorithm}{\Block{Algorithm.}}
\newcommand{\Correctness}{\Block{Correctness sketch.}}
\newcommand{\Complexity}{\Block{Complexity.}}

\newcommand{\ProblemDivider}{\vspace{0.6em}\hrule\vspace{0.8em}}

